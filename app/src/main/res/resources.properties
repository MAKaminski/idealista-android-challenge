# Names the language the unqualified `values/` folder is written in. AGP needs it to generate the
# locale config: without it the default resources would be an unlabelled entry in the system's
# per-app language list.
unqualifiedResLocale=en-US
